"""
==========================================
Landlord App — No Serializers Defined Here
==========================================
All serializers are centralized in the `core` app to avoid duplication.

Import serializers from `core.serializers`:
    from core.serializers import LandlordProfileSerializer, PropertySerializer,
                                  RentalRequestSerializer, MeetingSerializer,
                                  LeaseSerializer, PaymentSerializer
==========================================
"""
# No serializers defined here — all serializers live in core/serializers.py

