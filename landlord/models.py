"""
==========================================
Landlord App — No Models Defined Here
==========================================
All models are centralized in the `core` app to avoid duplication
and maintain a single source of truth.

Import models from `core.models`:
    from core.models import User, Landlord, Tenant, Property, Lease,
                             Payment, Maintenance, Notice, RentalRequest,
                             Meeting, PasswordResetCode
==========================================
"""
# No models defined here — all models live in core/models.py
# This file exists only to keep the Django app registry happy.
