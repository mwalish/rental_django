"""
Propeties App Models
=====================
Note: The app name "propeties" is a typo (should be "properties"),
but is kept as-is to avoid breaking existing migrations and database references.

All property models are defined in `core.models`.

Import models from core:
    from core.models import Property, Landlord
"""

